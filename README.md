# IT4062_The-price-is-right-Game

**IT4062: Network Programming - Project Game "Hãy Chọn Giá Đúng"**

Dự án được phát triển bằng C/C++ sử dụng socket programming (POSIX socket API) để xây dựng trò chơi mạng đa người chơi theo format chương trình "The Price is Right" (Hãy Chọn Giá Đúng). 

## 🎯 Đặc điểm kỹ thuật

- **Client**: Qt 5.15+ với QML/QtQuick (C++ backend + C socket layer)
- **Server**: Pure C với POSIX sockets, pthread, MySQL C API
- **Protocol**: Custom Message-based protocol qua TCP
- **Database**: MySQL 8.0+ (chạy qua XAMPP)
- **Platform**: Linux (Ubuntu 20.04+)

## 📁 Cấu trúc dự án

```
IT4062_The-price-is-right-Game/
├── client/                      # Ứng dụng client (Qt/QML)
│   ├── headers/
│   │   ├── BackEnd.h           # Qt C++ wrapper class (QObject)
│   │   └── Utils.h             # C socket functions declarations
│   ├── src/
│   │   ├── main.cpp            # Entry point, register QML types
│   │   ├── BackEnd.cpp         # Backend implementation
│   │   └── Utils.c             # Socket implementations (connect, signup, etc.)
│   ├── qml/
│   │   ├── main.qml            # Main window (800x600)
│   │   ├── HomeGuest.qml       # Home screen với sunburst animation
│   │   └── RegisterPage.qml    # Signup form
│   ├── resources/
│   │   └── qml.qrc             # Qt resource file
│   ├── client.pro              # qmake project file
│   └── Makefile                # Generated by qmake
│
├── server/                      # Server application (Pure C)
│   ├── include/
│   │   ├── server.h            # Server declarations, Message protocol
│   │   └── database.h          # MySQL wrapper functions
│   ├── src/
│   │   ├── main.c              # Entry point
│   │   ├── server.c            # Socket server, threading, client handling
│   │   └── database.c          # MySQL connection & queries
│   ├── config                  # Server configuration
│   ├── Makefile                # Build configuration
│   └── bin/                    # Build output directory
│       └── server              # Executable
│
├── database/                    # Database schema & seed data
│   ├── init.sql                # Table schemas (11 tables)
│   └── seed_data.sql           # Test users data
│
└── README.md

```

## 🔧 Yêu cầu hệ thống

### Dependencies

```bash
# Qt5 development tools
sudo apt install qt5-qmake qtbase5-dev qtdeclarative5-dev

# Qt5 QML runtime modules
sudo apt install qml-module-qtquick2 qml-module-qtquick-controls2 \
                 qml-module-qtquick-layouts qml-module-qtquick-window2

# MySQL client library (for server)
sudo apt install libmysqlclient-dev

# Build tools
sudo apt install gcc g++ make
```

### XAMPP MySQL Setup

1. **Cài đặt XAMPP**:
   ```bash
   # Download từ https://www.apachefriends.org/download.html
   sudo chmod +x xampp-linux-x64-*-installer.run
   sudo ./xampp-linux-x64-*-installer.run
   ```

2. **Khởi động MySQL**:
   ```bash
   sudo /opt/lampp/lampp startmysql
   ```

3. **Tạo database**:
   ```bash
   /opt/lampp/bin/mysql -u root
   ```
   ```sql
   CREATE DATABASE hay_chon_gia_dung CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   USE hay_chon_gia_dung;
   SOURCE /path/to/database/init.sql;
   SOURCE /path/to/database/seed_data.sql;
   ```

## 🚀 Build & Run

### 1. Build Server

```bash
cd server
make clean && make
```

Output: `bin/server` executable

### 2. Build Client

```bash
cd client
qmake client.pro
make
```

Output: `client` executable

### 3. Run Application

**Bước 1: Khởi động MySQL (nếu chưa chạy)**
```bash
sudo /opt/lampp/lampp startmysql
```

**Bước 2: Chạy Server**
```bash
cd server
./bin/server
```

Server sẽ:
- Kết nối MySQL database 'hay_chon_gia_dung'
- Listen trên port 5555
- Sẵn sàng nhận connections

**Bước 3: Chạy Client (terminal khác)**
```bash
cd client
./client 127.0.0.1 5555
```

Arguments:
- `127.0.0.1`: Server IP address
- `5555`: Server port

## 🎮 Chức năng hiện tại

### ✅ Đã hoàn thành
- [x] Kết nối client-server qua TCP socket
- [x] Message-based protocol (enum msg_type, struct Message)
- [x] Multi-threaded server (pthread per client connection)
- [x] User registration (SIGNUP)
  - Kiểm tra username đã tồn tại
  - Insert user mới vào MySQL
  - Response với SIGNUP_SUCCESS hoặc ACCOUNT_EXIST
- [x] UI Registration form
  - Username, Email, Password, Confirm Password fields
  - Agreement checkbox validation
  - Animated sunburst background

### 🚧 Đang phát triển
- [ ] User login 
- [ ] Game logic (chọn giá sản phẩm)
- [ ] Multiplayer room system

## 📡 Message Protocol

Client và server giao tiếp qua struct `Message`:

```c
enum msg_type {
    DISCONNECT,
    LOGIN, LOGIN_SUCCESS, LOGGED_IN, 
    WRONG_PASSWORD, ACCOUNT_NOT_EXIST, ACCOUNT_BLOCKED,
    SIGNUP, ACCOUNT_EXIST, SIGNUP_SUCCESS,
    CHANGE_PASSWORD, SAME_OLD_PASSWORD, CHANGE_PASSWORD_SUCCESS,
    LOGOUT
};

typedef struct _message {
    enum msg_type type;        // Message type
    char data_type[25];        // "string", "int", etc.
    int length;                // Length of value
    char value[BUFF_SIZE];     // Data payload (e.g., "username password")
} Message;
```

### Signup Flow:
1. Client gửi: `{type: SIGNUP, value: "username password"}`
2. Server xử lý: `handle_signup()` → query MySQL
3. Server phản hồi: `{type: SIGNUP_SUCCESS}` hoặc `{type: ACCOUNT_EXIST}`
4. Client hiển thị popup tương ứng

## 🗄️ Database Schema

Database `hay_chon_gia_dung` gồm 11 tables:

- **users**: user_id, username, password_hash, email, is_online, created_at, last_login_at
- **products**: product_id, name, actual_price, image_url, category
- **rooms**: room_id, created_by, status, max_players, created_at
- **room_members**: room_id, user_id, joined_at
- **matches**: match_id, room_id, status, started_at, ended_at
- **rounds**: round_id, match_id, round_number, status
- **round_products**: round_product_id, round_id, product_id
- **round_answers**: answer_id, round_id, user_id, guessed_price, is_correct
- **invitations**: invitation_id, from_user_id, to_user_id, room_id, status
- **match_events**: event_id, match_id, event_type, event_data, created_at
- **user_stats**: user_id, total_matches, wins, losses, accuracy_rate

Test users (password: `123456`):
- nhung, duyen, ha, yen, guest1

## 🐛 Troubleshooting

### Client không kết nối được server
```bash
# Kiểm tra server có đang chạy không
ps aux | grep server

# Kiểm tra port 5555
netstat -tuln | grep 5555
```

### Build client lỗi "qmake: command not found"
```bash
sudo apt install qt5-qmake qtbase5-dev
```

### Server lỗi MySQL connection
```bash
# Kiểm tra MySQL đang chạy
sudo /opt/lampp/lampp status

# Kiểm tra socket path trong database.c
# Phải là: /opt/lampp/var/mysql/mysql.sock
```

### Client lỗi "Could not find Qt platform plugin wayland"
- Warning này không ảnh hưởng, client vẫn chạy bình thường với X11 fallback

## 👥 Team Members

- **Duyên**: Registration system (signup feature)
- **[Tên bạn]**: Login system
- **[Tên bạn]**: Game logic
- **[Tên bạn]**: UI/UX

## 📝 License

Dự án môn học IT4062 - Network Programming

---

**Note**: Đây là phiên bản đang phát triển. Một số tính năng game logic chưa được implement.


